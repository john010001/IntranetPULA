<div class="header-box fixed-top">
    <div class="d-flex justify-content-between">
        <div class="logo-premier">
            <a href='@Url.Action("index", "home", null)'>
                <img src="~/content/img/logo/logo-plusultra-full-color.svg" alt="" style="width: 248px; height: 50px;">
            </a>
        </div>
        <div class="user-access d-flex align-items-center">
            @{
                var userClaims = User.Identity as System.Security.Claims.ClaimsIdentity;

                
                string nombre="";
                //You get the user’s first and last name below:

                try
                {
                    nombre = userClaims.FindFirst("name").Value;
                }
                catch (Exception)
                {

                    Response.RedirectToRoute("Default", new { controller = "", action = "" });
                }




                //  Intranet.ModelsApp.USUARIO _Usuario = (Intranet.ModelsApp.USUARIO)Session["USUARIO"];

                //   if (_Usuario == null)
                //{
                //    Response.RedirectToRoute("Default", new { controller = "", action = "" });

                //    //Response.Redirect("https://localhost:44368/", true);
                //}
                //else
                //{

                <span class="header-user-name mr-2 d-none d-xl-block">@nombre</span>
                <div class="dropdown">
                    <a href="#!" class="header-user-menu-link" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" data-offset="10,10" id="user-menu"><i class="fas fa-user"></i></a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="link-consultas">
                        <span class="dropdown-menu-arrow dropdown-user-menu-arrow"></span>
                        <h6 class="dropdown-header">
                            @nombre
                        </h6>
                        @*<a class="dropdown-item" href="#!"><i class="fas fa-cogs mr-2"></i>Ajustes</a>*@
                        @*<a class="dropdown-item" href="#!"><i class="fas fa-key mr-2"></i>Cambiar contraseña</a>*@
                        <a class="dropdown-item" href='@Url.Action("SignOut", "Home")'><i class="fas fa-sign-out-alt mr-2"></i>Cerrar Sesión</a>
                    </div>
                </div>
                <div class="d-xl-none ml-2">
                    <a class="js-sidebar--open sidebar-trigger" href="#!"><i class="fas fa-bars"></i></a>
                </div>
                @*}*@
            }
        </div>
    </div>
</div>