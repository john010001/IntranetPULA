
@{
    ViewBag.Title = "";
    Layout = "~/Views/Shared/LayoutAside.cshtml";
}



    <div class="container-fluid py-4">        
        <h1 class="admin-big-title">Acceso Azure</h1>
        <span class="gn-orange-line mb-4"></span>
        <div class="row">
            <div class="col col-12 mb-4">
                <div class="card admin-card">
                    <div class="card-header">Main Claims:</div>
                    <div class="p-3">
                        @*<table class="table table-striped table-bordered table-hover">
                            <tr><td>Name</td><td>@ViewBag.Name</td></tr>
                            <tr><td>Username</td><td>@ViewBag.Username</td></tr>
                            <tr><td>Subject</td><td>@ViewBag.Subject</td></tr>
                            <tr><td>TenantId</td><td>@ViewBag.TenantId</td></tr>
                        </table>*@

                        <table class="table table-bordered table-responsive table-hover table-striped datatable display" id="tb_afiliado">
                            <thead>
                                <tr>
                                    <th>
                                        Name
                                    </th>
                                    <th>
                                        Username
                                    </th>
                                    <th>
                                        Subject
                                    </th>
                                    <th>
                                        TenantId 1
                                    </th>
                                </tr>
                            </thead>
                            <tbody>                            
                                <tr>
                                    <td>@ViewBag.Name</td>
                                    <td>@ViewBag.Username</td>
                                    <td>@ViewBag.Subject</td>
                                    <td>@ViewBag.TenantId</td>
                                </tr>
                            </tbody>
                        </table>


                        @using (Html.BeginForm())
                        {
                            <div class="row">

                                @*<div class="col col-12 col-md-6 col-lg-3">
                    <div class="form-group">
                        <label for="input1">ID de afiliado:</label>
                        <input type="text" name="IdAfiliado" id="IdAfiliado" class="form-control validate">

                    </div>
                </div>*@
                            </div>
                        }
                    </div>
                </div>
            </div>
            <div class="col col-12 mb-4">
                <div class="card admin-card">


                    <div>
                        <div class="p-3 datatable-box">
                            <div class="card-header">All Claims:</div>
                            
                            <table class="table table-striped table-bordered table-hover table-condensed table-responsive">
                                @foreach (var claim in System.Security.Claims.ClaimsPrincipal.Current.Claims)
                                {
                                    <tr><td>@claim.Type</td><td>@claim.Value</td></tr>
}
                            </table>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>



@*<table class="table table-striped table-bordered table-hover">
    <tr><td>Name</td><td>@ViewBag.Name</td></tr>
    <tr><td>Username</td><td>@ViewBag.Username</td></tr>
    <tr><td>Subject</td><td>@ViewBag.Subject</td></tr>
    <tr><td>TenantId</td><td>@ViewBag.TenantId</td></tr>
</table>
<br />
<h3>All Claims:</h3>
<table class="table table-striped table-bordered table-hover table-condensed">
    @foreach (var claim in System.Security.Claims.ClaimsPrincipal.Current.Claims)
    {
<tr><td>@claim.Type</td><td>@claim.Value</td></tr>}
</table>*@